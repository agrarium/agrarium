{
  "private": true,
  "name": "@agrarium/test-helpers",
  "version": "0.0.2",
  "description": "Set of helpers for Agrarium tests",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf lib && tsc",
    "lint": "tsc -p . --noEmit && tslint -p . -t stylish",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@agrarium/core": "^0.1.0",
    "@types/agrarium": "*",
    "stream-to-array": "^2.3.0"
  },
  "devDependencies": {
  }
}
